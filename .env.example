# Copy this file to `.env` in the project root and fill in your key.
# `.env` is gitignored — never commit real secrets (public GitHub).
#
# Get a key: https://openrouter.ai/keys

OPENROUTER_API_KEY=sk-or-v1-your-key-here
